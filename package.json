{
  "name": "gsap-sandbox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "build:runtime": "npm --prefix packages/runtime run build",
    "sync:runtime-assets": "node scripts/copy-runtime-assets.mjs",
    "build:all": "npm run build && npm run build:runtime && npm run sync:runtime-assets",
    "build:netlify": "pnpm run build:all",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "dompurify": "^3.4.3",
    "gsap": "^3.15.0",
    "marked": "^18.0.3",
    "vue": "^3.5.34",
    "vue-router": "^5.0.7"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "@vitejs/plugin-vue": "^6.0.6",
    "@vue/tsconfig": "^0.9.1",
    "sass": "^1.99.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.2.8"
  }
}
